spring:
  application:
    name: sai-demo
  ai:
    ollama:
      init:
        pull-model-strategy: never
        timeout: 5m
        max-retries: 0
      base-url: http://localhost:11434
      chat:
        model: llama3.2
      embedding:
        model: mxbai-embed-large

